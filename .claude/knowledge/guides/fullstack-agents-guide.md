# Fullstack Agents 使用指南

本指南說明如何使用三個專業 AI agents（`/fe`、`/be`、`/db`）來加速全端開發。

## 目錄

- [快速開始](#快速開始)
- [三個 Agents 介紹](#三個-agents-介紹)
- [使用技巧](#使用技巧)
- [完整開發流程](#完整開發流程)
- [常見問題](#常見問題)

---

## 快速開始

### 什麼時候用哪個 Agent？

```
💡 UI / React / Tailwind 問題         → /fe (Frontend Engineer)
💡 API / 後端架構 / 權限               → /be (Backend Architect)
💡 資料庫 / Schema / Migration         → /db (Database Migrator)
```

### 基本使用方式

直接在對話中輸入指令 + 需求描述：

```bash
/fe 建立一個使用者資料卡片 component
/be 設計 /api/users 的 CRUD API
/db 為 users 表格新增 email 欄位
```

---

## 三個 Agents 介紹

### 1. `/fe` - Frontend Engineer（前端工程師）

**專長領域：**
- React / Next.js 頁面開發
- TailwindCSS 樣式與 RWD
- TypeScript 型別定義
- 前端測試（Vitest / Jest）
- API 串接與狀態管理

**輸出內容：**
1. 變更概要說明
2. 要修改的檔案清單
3. 完整程式碼實作
4. 使用範例（如果是新 component）
5. 測試建議

**實用範例：**

```bash
# 範例 1：重構現有頁面
/fe 幫我重構登入頁，改用 React Hook Form，加上錯誤訊息顯示

# 範例 2：建立新 Component
/fe 建立 DateRangePicker component，需支援 Tailwind、受控模式

# 範例 3：API 整合
/fe 將 /api/users 整合到 UserList，加上 loading 和錯誤處理

# 範例 4：TypeScript 強化
/fe 將選取的程式碼改成 strict TypeScript，加上單元測試

# 範例 5：響應式設計
/fe 把 Dashboard layout 改成 RWD，手機上顯示單欄
```

**Agent 會主動詢問的問題：**
- 設計風格偏好？（Material / Ant Design / Custom）
- 狀態來源？（local / global / server）
- API 格式？（REST / GraphQL）
- 需要 loading / error handling 嗎？

---

### 2. `/be` - Backend Architect（後端架構師）

**專長領域：**
- REST / GraphQL API 設計
- Authentication / Authorization（RBAC）
- API 文件（OpenAPI / Swagger）
- Middleware 與錯誤處理
- Logging / Metrics / Tracing
- 效能優化與快取

**輸出內容：**
1. 端點清單與資料流
2. Request/Response Schema
3. 驗證規則
4. 完整程式碼實作
5. API 文件（Markdown 或 OpenAPI）
6. 測試建議

**實用範例：**

```bash
# 範例 1：設計 CRUD API
/be 設計 /api/orders 的 CRUD，支援 RBAC（admin/user）

# 範例 2：加強錯誤處理
/be 為選取的程式碼加上結構化 log 和錯誤處理

# 範例 3：權限系統
/be 設計 RBAC 系統，支援 admin/manager/user 三種角色

# 範例 4：API 文件
/be 為 /api/products 生成 OpenAPI 3.0 文件

# 範例 5：Middleware
/be 建立 rate limiting middleware，每 IP 每分鐘最多 60 次

# 範例 6：輸入驗證
/be 為 /api/auth/register 加上完整驗證（email、密碼強度）
```

**Agent 會自動處理：**
- 偵測後端框架（Express / NestJS / FastAPI 等）
- 遵循框架最佳實務
- 提供錯誤碼與訊息定義
- 建議權限矩陣（如涉及 RBAC）

---

### 3. `/db` - Database Migrator（資料庫工程師）

**專長領域：**
- PostgreSQL / MySQL Schema 設計
- ORM Migration（Prisma / TypeORM）
- 索引設計與查詢優化
- 零停機 Migration 策略
- 資料遷移與轉換
- 資料庫關聯設計

**輸出內容（標準四步驟）：**
1. **評估** - 影響範圍、風險分析
2. **計畫** - 步驟說明、執行順序
3. **Migration Script** - SQL 或 ORM 程式碼
4. **Rollback Script** - 回滾方案

**實用範例：**

```bash
# 範例 1：新增欄位（零停機）
/db 為 orders 加上 status 欄位（enum），零停機 migration

# 範例 2：查詢優化
/db 分析這個 SELECT 查詢，建議索引和重寫方式

# 範例 3：建立關聯
/db 建立 users 和 posts 的一對多關係，含外鍵和索引

# 範例 4：資料型別轉換
/db 把 birth_date 從 VARCHAR 改成 DATE，含資料轉換

# 範例 5：索引策略
/db products 常用 category+price 篩選和 name 搜尋，設計索引

# 範例 6：Schema 設計
/db 設計電商訂單系統 schema（訂單、商品、付款、配送）
```

**重要安全機制：**
- ⚠️ 破壞性操作（DROP / DELETE）會先顯示計畫，要求確認
- 🔒 不會直接執行 destructive SQL
- 📋 總是提供 rollback 方案
- 🧪 建議測試步驟

---

## 使用技巧

### 1. 選取程式碼區塊

在執行指令前，先選取相關程式碼：

```bash
# 步驟 1：選取 UserCard.tsx 的某段程式碼
# 步驟 2：執行指令
/fe 將選取的程式碼改成 TypeScript strict mode
```

**好處：**
- Agent 會聚焦在選取的區塊
- 更精準的變更建議
- 減少不必要的探索時間

### 2. 提供清楚的需求

**❌ 不好的範例：**
```bash
/fe 做一個表單
```

**✅ 好的範例：**
```bash
/fe 建立使用者註冊表單，需要：
- 欄位：email, password, confirmPassword, name
- 使用 React Hook Form
- 驗證：email 格式、密碼至少 8 碼
- 錯誤訊息要顯示在欄位下方
- Tailwind 樣式
```

### 3. 組合使用 Agents

開發新功能時，建議依序使用：

```bash
# 步驟 1：設計資料結構
/db 設計 attendance 系統的 schema（users, check_ins, locations）

# 步驟 2：實作 API
/be 為 attendance 建立 CRUD API，支援日期範圍查詢

# 步驟 3：建立前端
/fe 建立 attendance 打卡頁面，整合 /api/attendance 端點
```

### 4. 迭代優化

Agent 的回應可以繼續追問：

```bash
/fe 建立 UserCard component
# Agent 回應後...

# 追問 1
請加上 loading skeleton

# 追問 2
這個可以加上 hover 效果嗎？

# 追問 3
幫我補上 Vitest 單元測試
```

### 5. 指定技術細節

明確指出使用的技術棧：

```bash
/fe 使用 React 18 + TypeScript + Tailwind 建立...
/be 使用 Express + TypeScript + Zod 驗證...
/db 使用 PostgreSQL 14 + Prisma ORM...
```

---

## 完整開發流程

### 情境：開發「使用者評論」功能

#### Phase 1：資料庫設計

```bash
/db 設計評論系統的 schema：
- comments 表格（id, user_id, post_id, content, created_at）
- 需要支援：按 post_id 查詢、按時間排序
- 外鍵約束到 users 和 posts
- 適當的索引策略
```

**預期輸出：**
- Schema 設計說明
- CREATE TABLE SQL
- CREATE INDEX SQL
- Migration 檔案（如使用 ORM）
- Rollback script

#### Phase 2：後端 API

```bash
/be 為評論系統設計 API：
- POST /api/comments - 新增評論
- GET /api/comments?post_id=X - 取得某篇文章的評論
- DELETE /api/comments/:id - 刪除評論（僅作者或 admin）
- 需要驗證使用者身份
- 輸入驗證（content 必填、長度限制）
```

**預期輸出：**
- 端點清單與說明
- Request/Response Schema
- 驗證規則
- 權限檢查邏輯
- 完整程式碼實作
- API 文件

#### Phase 3：前端介面

```bash
/fe 建立評論區 component：
- CommentList：顯示評論列表（含作者頭像、時間）
- CommentForm：新增評論表單
- 整合 /api/comments 端點
- Loading 和 error handling
- 使用 TypeScript
- Tailwind 樣式
```

**預期輸出：**
- Component 程式碼
- TypeScript 型別定義
- API 串接邏輯
- 使用範例
- 測試建議

#### Phase 4：優化與測試

```bash
# 資料庫優化
/db 分析 comments 的查詢模式，評估是否需要額外索引

# 後端測試
/be 為 /api/comments 端點補上整合測試

# 前端測試
/fe 為 CommentList 和 CommentForm 補上單元測試
```

---

## 常見問題

### Q1: Agent 回應不符合預期怎麼辦？

**A1:** 提供更多細節

```bash
# 原始請求
/fe 建立表單

# 改進後
/fe 建立表單，使用 React Hook Form + Zod 驗證
欄位：name（必填）、email（必填、格式驗證）、message（選填）
提交後顯示成功訊息，錯誤時顯示紅色提示
```

### Q2: 如何讓 Agent 遵循專案規範？

**A2:** 明確指出規範

```bash
/fe 建立 component，遵循以下規範：
- 使用 4-space indent
- TypeScript strict mode
- Props 一律使用 interface 定義
- 檔案命名使用 PascalCase
```

或者參考知識庫：
```bash
/fe 依照 .claude/knowledge/references/frontend-best-practices.md
的規範建立 UserProfile component
```

### Q3: Agent 可以修改現有程式碼嗎？

**A3:** 可以，有兩種方式

**方式 1：選取程式碼**
1. 選取要修改的程式碼區塊
2. 執行指令（如 `/fe 重構選取的程式碼...`）

**方式 2：指定檔案**
```bash
/fe 重構 src/components/UserCard.tsx，改用 functional component
```

### Q4: 如何處理大型重構？

**A4:** 分階段進行

```bash
# 階段 1：規劃
請列出重構 UserList component 的步驟，但先不要執行

# 階段 2：逐步執行
/fe 執行重構步驟 1：將 class component 改成 functional component

# 階段 3：測試
/fe 為重構後的 component 補上測試

# 階段 4：驗證
跑一次測試，確認沒問題
```

### Q5: Agent 會自動執行破壞性操作嗎？

**A5:** 不會，`/db` 有安全機制

當涉及以下操作時：
- `DROP TABLE` / `DROP COLUMN`
- `DELETE` 大量資料
- 改變資料型別（可能遺失資料）

Agent 會：
1. 先顯示「評估」和「計畫」
2. **不會直接給出 SQL**
3. 要求您明確確認
4. 確認後才提供實際 SQL 和 rollback script

### Q6: 可以同時使用多個 Agents 嗎？

**A6:** 可以，但建議依序使用

**❌ 不建議：**
```bash
/fe /be /db 一起處理這個功能
```

**✅ 建議：**
```bash
# 先資料庫
/db 設計 schema

# 等完成後，再後端
/be 實作 API

# 最後前端
/fe 建立介面
```

### Q7: Agent 的回應太長怎麼辦？

**A7:** 要求分段輸出

```bash
/fe 建立評論系統的完整介面
但請分成三個部分：
1. 先給 CommentList
2. 再給 CommentForm
3. 最後給整合範例
```

### Q8: 如何讓 Agent 提供測試？

**A8:** 明確要求

```bash
/fe 建立 UserCard component，並提供：
1. Component 程式碼
2. TypeScript 型別
3. Vitest 單元測試（至少 3 個 test cases）
```

或者事後追加：
```bash
# Agent 回應後
請為這個 component 補上單元測試
```

---

## 進階技巧

### 1. 使用 Git 配合開發

```bash
# 開發前先 commit
git add .
git commit -m "feat: before adding comments feature"

# 使用 agents 開發
/db 設計 comments schema
/be 實作 comments API
/fe 建立 comments UI

# 檢視變更
git diff

# 如果滿意就 commit
git add .
git commit -m "feat: add comments feature"
```

### 2. 建立可重用的 Prompts

將常用的需求儲存起來：

```bash
# 建立 .claude/prompts/new-crud-api.md
當我說「建立 CRUD API for {resource}」時：
1. 設計 RESTful 端點（GET/POST/PUT/DELETE）
2. 加上輸入驗證（使用 Zod）
3. 加上 RBAC（admin 全權限，user 只能操作自己的資料）
4. 提供 OpenAPI 文件
5. 建議測試案例
```

然後使用：
```bash
/be 依照 .claude/prompts/new-crud-api.md 為 products 建立 CRUD API
```

### 3. 整合 CI/CD

```bash
# 開發完成後
/be 幫我檢查 API 端點是否符合 OpenAPI 規範

/fe 確認所有 components 都有 TypeScript 型別

/db 檢查是否有未執行的 migrations
```

---

## 相關資源

- [指令參考](../../commands/fullstack-agents.md) - 完整的指令說明
- [前端最佳實務](../references/frontend-best-practices.md)
- [後端最佳實務](../references/backend-best-practices.md)
- [資料庫最佳實務](../references/database-best-practices.md)

---

*最後更新：2025-11-19*
